<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/music_selection_item_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
     >
    
   <!-- <ImageView
        android:id="@+id/music_selection_item_imageview"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:contentDescription="@string/selection_music_item_imageview_description"
        android:src="@drawable/ic_launcher" />
        -->
    <TextView
        android:id="@+id/music_selection_item_textview"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:drawableLeft="@drawable/ic_launcher"
        android:drawablePadding="15dp"
        android:gravity="center_vertical"
        style="@android:style/TextAppearance.Medium"
        />

</LinearLayout>
